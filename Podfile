platform :ios, '13.0'
use_frameworks!

target 'baraka-iOS' do
  # Specify exact semantic versions to avoid “Malformed version” error
  pod 'RxSwift', '~> 6.9.0'
  pod 'RxCocoa', '~> 6.9.0'
  
  target 'baraka-iOSTests' do
    inherit! :search_paths
  end

  target 'baraka-iOSUITests' do
  end
end
